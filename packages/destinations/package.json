{
  "name": "@timeaway/destinations",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "data:climate": "tsx scripts/fetch-climate.ts",
    "data:codegen": "tsx scripts/codegen.ts"
  },
  "dependencies": {
    "@timeaway/shared": "workspace:*",
    "@timeaway/trip-engine": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
